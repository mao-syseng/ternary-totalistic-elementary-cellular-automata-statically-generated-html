---
title:ternary-totalistic-elementary-cellular-automata-statically-generated-html
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Explore all rulesets of a ternary totalistic elementary cellular automata, rendered in pure html"
    />
    <style>
         * {
        font-family: monospace;
        font-size: large;
        color: black;
        background: seashell;
      }

      @media (prefers-color-scheme: dark) {
        * {
          color: seashell;
          background: black;
        }
      }
      body {
        margin: 32px;
        display: flex;
        gap: 16px;
      }
      .ca-row {
        white-space: nowrap;
        font-size: 20px;
        line-height: 0.8;
        letter-spacing: -4px;
      }
      .ca-row span {
        display: inline-block;
        text-align: center;
      }
      .overview {
        display: flex;
        max-width: 100vw;
        flex-wrap: wrap;
        gap: 16px;
      }
    </style>
    <title>{{ title }}</title>
  </head>
  <body>
    <main>
      {{ content | safe }}
    </main>
  </body>
</html>
